- hosts: localhost
  connection: local
  become: false
  gather_facts: false
  tasks:
    - name: Reject obsolete backup setup operations
      ansible.builtin.fail:
        msg: Backup schedules are managed by Scheduler and Operations. Run migrate-backup-cron on the original host.
