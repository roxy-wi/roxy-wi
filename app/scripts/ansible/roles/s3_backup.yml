- hosts: localhost
  connection: local
  become: false
  gather_facts: false
  tasks:
    - name: Reject obsolete S3 backup setup operations
      ansible.builtin.fail:
        msg: S3 backups use boto3 through Operations. Run migrate-backup-cron on the original host.
