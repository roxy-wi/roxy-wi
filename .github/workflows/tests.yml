name: Tests

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

permissions:
  contents: read

jobs:
  unit-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        python-version: ['3.10', '3.11', '3.12']

    steps:
      - name: Check out repository
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt -r requirements-dev.txt

      - name: Compile Python sources
        run: python -m compileall -q app tests scheduler_runner.py service_event_consumer.py roxy_wi.py rotate_credential_secret.py

      - name: Smoke test application startup
        run: python -m pytest tests/smoke -q

      - name: Run distributed service integration tests
        run: python -m pytest tests/integration/test_service_events.py tests/integration/test_operation_queue.py -q

      - name: Validate container manifests
        run: |
          docker compose --env-file docker/.env.example -f docker/docker-compose.yml config --quiet
          docker compose --env-file docker/.env.example -f docker/docker-compose.sqlite.yml config --quiet

      - name: Run unit and security tests
        run: python -m pytest --ignore=tests/smoke --cov=app --cov-report=term-missing --cov-fail-under=25
